{
  "name": "spike-whatsapp-baileys",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Spike de validação: pareamento WhatsApp via Baileys com QR no terminal",
  "scripts": {
    "start": "node index.js",
    "install:isolated": "pnpm install --ignore-workspace",
    "reset": "rm -rf auth && echo 'Sessão resetada. Na próxima execução vai pedir QR de novo.'"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@whiskeysockets/baileys": "latest",
    "pino": "^9.0.0",
    "qrcode-terminal": "^0.12.0"
  }
}
