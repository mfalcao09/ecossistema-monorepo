# Sessão 088 — 13/04/2026 — Diagnóstico Causa Raiz Webhook WhatsApp

## Contexto
Continuação das sessões 086-087 (Sprint 2 Atendimento): webhook `/api/atendimento/webhook` em `gestao.ficcassilandia.com.br` retornava 200 OK mas **0 rows** gravadas no Supabase. Marcelo tentou múltiplas vezes via Meta dispatcher ("Testar" em v24.0) com Phone Number ID `938274582707248` (WABA FIC `1833772130611929`, número `+55 67 93618-0058`).

## Investigação (ordem cronológica)

### Hipótese 1 — RLS bloqueando (descartada)
- Substituí client por `createAdminClient()` (service_role) em sessão anterior — webhook agora bypassa RLS.

### Hipótese 2 — NOT NULL em tenant_id (parcialmente correta)
- Editei webhook.ts para popular `tenant_id` a partir de `chat_channels` (fallback `'00000000-0000-0000-0000-000000000001'`).
- Adicionei `tenant_id` nos INSERTs de `chat_contacts`, `chat_conversations`, `chat_messages`.
- Deploy `dpl_4rZc3weyDEjhscuQCgFrAGK61Hce` (commit `994c299`) + `715b516` (debug endpoint) — ambos READY.

### Hipótese 3 — PostgREST schema cache stale (descartada)
- Criei `GET /api/atendimento/debug-write` para expor erro real no JSON body.
- Resposta: `{"hasUrl":true,"hasServiceKey":true,"serviceKeyLen":219,"insert":{"ok":false,"error":{"message":"Could not find the table 'public.chat_contacts' in the schema cache","code":"PGRST205","hint":"Perhaps you meant the table 'public.atendimento_contacts'"}}}`
- Tentei `NOTIFY pgrst, 'reload schema'` + `reload config` + `COMMENT ON TABLE` — sem efeito.
- Curl direto no PostgREST do projeto `bvryaopfjiyxjgsuhjsb` com anon key: **200 OK** retornando `[]` para `chat_contacts` → cache estava OK; problema era outro projeto.

### 🎯 CAUSA RAIZ REAL — Projeto Supabase errado

**O Vercel `prj_VIEmyVHGD61ow5uf5pmBJp5W7eAX` (diploma-digital) aponta para Supabase `ifdnjieklngcfodmtied` (diploma-digital, sa-east-1), NÃO para `bvryaopfjiyxjgsuhjsb` (Intentus, us-west-2).**

Evidências:
1. O hint do PostgREST "Perhaps you meant `atendimento_contacts`" — essa tabela NÃO existe em `bvryaopfjiyxjgsuhjsb`, mas EXISTE em `ifdnjieklngcfodmtied`.
2. Projeto `ifdnjieklngcfodmtied` tem 9 tabelas `atendimento_*` aplicadas pela migration `20260412_atendimento_modulo_init.sql` (Sprint 1 sessão 085, 12/04/2026).
3. Eu tinha aplicado a migração `chat_*` (~14 tabelas) no projeto ERRADO `bvryaopfjiyxjgsuhjsb` (Intentus).

## Schema correto (em produção no projeto certo)

Migration canônica: `supabase/migrations/20260412_atendimento_modulo_init.sql`

Tabelas:
- `atendimento_inboxes` — caixas por canal (phone_number_id em `provider_config` JSONB)
- `atendimento_contacts` — `name`, `phone_number`, `email`, `external_id`, `additional_attributes` JSONB
- `atendimento_conversations` — `inbox_id`, `contact_id`, `status` (open/resolved/pending/snoozed), `channel_conversation_id`, `last_activity_at`, `unread_count`
- `atendimento_messages` — `conversation_id`, `content`, `message_type` (incoming/outgoing/activity/template), `content_type`, `channel_message_id`, `sender_type` (contact/agent/bot/system)
- `atendimento_labels` (6 seeds: Matrícula, Financeiro, Acadêmico, Diploma, Secretaria, Urgente)
- `atendimento_conversation_labels`, `atendimento_agents`, `atendimento_automation_rules`, `atendimento_whatsapp_templates`

**Single-tenant (FIC), sem coluna `tenant_id`**, RLS simples `auth.uid() IS NOT NULL`.

Triggers prontos:
- `atendimento_update_conversation_on_message()` — atualiza `last_activity_at` e `unread_count` automaticamente após INSERT em `atendimento_messages`.
- `atendimento_set_updated_at()` em várias tabelas.

## Ação executada nesta sessão
✅ Seed do inbox WhatsApp FIC no projeto correto `ifdnjieklngcfodmtied`:
```
id = 179deb2b-e254-4125-9465-33acf2a8ed14
name = 'WhatsApp FIC (WABA)'
channel_type = 'whatsapp'
provider_config = {
  "phone_number_id": "938274582707248",
  "waba_id": "1833772130611929",
  "display_phone_number": "+55 67 93618-0058",
  "provider": "meta_cloud_api"
}
enabled = true
```

## Pendências (Opção A aprovada por Marcelo)

1. **Reescrever `src/app/api/atendimento/webhook/route.ts`** para usar schema `atendimento_*`:
   - Buscar inbox por `provider_config->>'phone_number_id'` em `atendimento_inboxes`
   - Contato: upsert em `atendimento_contacts` por `phone_number` (não `phone`)
   - Conversa: insert em `atendimento_conversations` com `inbox_id`, `contact_id`, `status='open'`, `channel_conversation_id=<wa_conv_id>`
   - Mensagem: insert em `atendimento_messages` com `message_type='incoming'`, `content_type='text'`, `channel_message_id=<wamid>`, `sender_type='contact'`
   - Remover toda lógica de `tenant_id` (não existe neste schema)

2. **Remover rota temporária `src/app/api/atendimento/debug-write/route.ts`** e bypass correspondente no `src/middleware.ts` linha 60.

3. **Commit + push via `/tmp` clone** (workaround FUSE bind-mount):
   - Usar token em `/sessions/nice-clever-ramanujan/mnt/GitHub/.github-token` (URL form com PAT embutido, formato `https://mfalcao09:PAT@github.com`)
   - Repo: `mfalcao09/diploma-digital` (NOT `ERP-Educacional`)
   - Conventional commit + `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>`

4. **Aguardar deploy Vercel READY** no `prj_VIEmyVHGD61ow5uf5pmBJp5W7eAX`.

5. **Validar**: Marcelo clica "Testar" no dispatcher Meta → `SELECT * FROM atendimento_contacts/conversations/messages ORDER BY created_at DESC`.

## Tech debt identificado

- **Projeto Supabase `bvryaopfjiyxjgsuhjsb` (Intentus) tem 14 tabelas `chat_*` órfãs** criadas por engano. Não usadas em lugar nenhum. Limpar em sessão futura.
- Duas convenções paralelas de módulo de atendimento: `chat_*` (Nexvy/ON AI — sessão 088 research externo) e `atendimento_*` (ERP FIC Sprint 1). ERP usa canonicamente `atendimento_*`.

## Deploys desta sessão
- `dpl_4rZc3weyDEjhscuQCgFrAGK61Hce` (commit `994c299`) — tenant_id fix — READY
- `715b516` — debug-write endpoint — READY
- **Último commit no main: `715b516`** (ainda com código errado usando `chat_*`)

## Referências
- Projeto Vercel ERP: `prj_VIEmyVHGD61ow5uf5pmBJp5W7eAX` team `team_Q0aWmmsHjFRW4cixrnefF7dW`
- Supabase ERP CORRETO: `ifdnjieklngcfodmtied` (diploma-digital, sa-east-1, v17.6.1.084)
- Supabase ERRADO onde apliquei `chat_*`: `bvryaopfjiyxjgsuhjsb` (Intentus, us-west-2)
- Supabase ECOSYSTEM (memória online): `gqckbunsfjgerbuiyzvn` (us-east-2)
- Migration canônica: `supabase/migrations/20260412_atendimento_modulo_init.sql`
- Sessão anterior: `sessao-087-2026-04-12.md`

## Próxima sessão
Implementar Opção A — reescrita do webhook.ts para schema `atendimento_*` + cleanup + deploy + validação Meta dispatcher.
