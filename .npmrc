# Hoisted node_modules layout — obrigatório para Metro bundler (Expo) em apps/jarvis-app.
# pnpm default (isolated/symlinked) quebra Metro porque ele só resolve via node_modules tradicional.
# Trade-off: node_modules maior por app, mas dedupe via pnpm store segue ativo.
node-linker=hoisted
